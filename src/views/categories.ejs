<%- include('partials/header') %>
<main>
    <h1><%= title%></h1>
    <p>Here are Categories and their associated Projects:</p>
    <ul>
        <% categories.forEach(category => { %>
            <li><strong><%= category.category %></strong></li>
        <% }); %>
    </ul>
</main>
<%- include('partials/footer')%>